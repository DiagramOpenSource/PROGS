|FICHEROS;

|FIN;

|VARIABLES;
     &ser_ped = "";
     &num_ped = 0;
     aAlfa = "";
     aProg = "";
     aParam = "";
|FIN;

|PROGRAMA;
     |SI ser_ped = "";
          aProg = PARAMETRO;
     |SINO;                   || Llama el agifa104/tagm0030
          aProg = "tagz0019";
          aParam = ";" + ser_ped + (("000000" + num_ped) % -106) + PARAMETRO;
     |FINSI;
     aAlfa = "|:dscomer9/" + aProg + " agif0041 " + #48#0 + aParam;
     |SUB_EJECUTA_NP aAlfa;
|FPRO;
