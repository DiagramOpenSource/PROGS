|VARIABLES;
     aEjecuta = "";
     aMensaje = "";
     aParametro = "";
|FIN;

|PROGRAMA;
     || Este programa recibe como parametro la aplicacion y el parametro/s que se
     || deben de ejecutar y simplemente los ejecuta.

     || Como ejemplos de parametros.

     || Para ejecutar en rwindex con parametro: AUTOSQL

     || ":/agitool/bin/rwindex;AUTOSQL"      TODO ES EL PARAMETRO QUE RECIBE
     || es decir que se ejecuta de esta forma, si se ejecutara desde el menu
     || {{aplihubb;:/agitool/bin/rwindex;AUTOSQL}}

     aParametro = PARAMETRO;
     |QBF aParametro;

||     aMensaje = "2405Estoy en aplihubb. Con Parametro: " + aParametro;
||     |MENAV aMensaje;
||     |MENSA aMensaje

     aEjecuta = aParametro;
     |SUB_EJECUTA aEjecuta;
|FPRO;
