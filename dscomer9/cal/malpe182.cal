|FICHEROS;
     malpe182 #0;
     agifa112 #1;
|FIN;

|VARIABLES;
     aAlfa = "";
|FIN;

|PROCESO Provedor;
     aAlfa = #1#14; ||Banco 4
     |QBT aAlfa;
     aAlfa = ("0000" + aAlfa) % -104;
     #1#14 = aAlfa;

     aAlfa = #1#15; ||Sucursal
     |QBT aAlfa;
     aAlfa = ("0000" + aAlfa) % -104;
     #1#15 = aAlfa;

     aAlfa = #1#13; ||Digito
     |QBT aAlfa;
     aAlfa = ("00" + aAlfa) % -102;
     #1#13 = aAlfa;

     aAlfa = #1#12; ||cuenta
     |QBT aAlfa;
     aAlfa = ("0000000000" + aAlfa) % -110;
     #1#12 = aAlfa;

     |GRABA 020#1a;
     |LIBERA #0;
|FPRC;

|DEFBUCLE Provedor;
     #1#1;
     ;
     #0#0;
     #0#1;
     be;
     NULL, Provedor;
|FIN;

|PROCESO Tipo3; |TIPO 3;
     |BUCLE Provedor;
|FPRC;
