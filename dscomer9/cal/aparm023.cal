|FICHEROS;
     aparm023 #0;
|FIN;

|VARIABLES;
     aAlfa = "";
|FIN;

|PROCESO CdPostal; |TIPO 0;
     aAlfa = (("00" + #0#4) % -102) + (("000" + #0#5) % -103);
     #0#6 = aAlfa;
|FPRC;
