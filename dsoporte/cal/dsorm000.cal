/*
|FICHEROS;
     dsorm000 #0;
|FIN;

|VARIABLES;
     aAlfa = "";
     aDire = "";

     nModo = 0;
|FIN;

|PROCESO Mkdir; |TIPO 3;
     aDire = "";
     |DIRECTORIO aDire;  |QBF aDire;

     nModo = (FEntrada / 100) ! 0;
     |SI nModo = 1;
          |DFICE aAlfa;  |QBF aAlfa;
          aAlfa = aAlfa + (("000000" + #0#0) % -106);
          |MKDIR aAlfa;
     |FINSI;
|FPRC;

|PROCESO Rmdir; |TIPO 2;
     aDire = "";
     |DIRECTORIO aDire;  |QBF aDire;

     nModo = (FEntrada / 100) ! 0;
     |SI nModo = 3;
          |DFICE aAlfa;  |QBF aAlfa;
          aAlfa = aAlfa + (("000000" + #0#0) % -106);
          aAlfa = aDire + "bin/agirm -r " + aAlfa;
          |SYSTEM aAlfa;
     |FINSI;
|FPRC;
*/

|PROCESO Tipo90;   |TIPO 8;
    |MENAV "0000 Error de version, pongase en contacto con su distribuidor";
    |PTEC 807;
|FPRC;
